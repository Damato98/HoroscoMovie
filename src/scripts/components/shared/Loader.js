import React, { Component } from 'react'
import LoaderContainer from '../../containers/shared/LoaderContainer'

class Loader extends Component {
    render() {
        return (
            <LoaderContainer />
        )
    }
}

export default Loader;